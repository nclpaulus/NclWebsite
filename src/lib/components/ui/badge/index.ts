/**
 * Composant `Badge` (UI) et helpers associés.
 */
export { default as Badge } from './badge.svelte';

/**
 * Variants Tailwind (`tailwind-variants`) + type de variantes supportées.
 */
export { badgeVariants, type BadgeVariant } from './badge.svelte';
